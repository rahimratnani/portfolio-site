import { Icon } from "@chakra-ui/react";
import { FaCaretRight } from "react-icons/fa";
const CaretIcon = () => {
  return <Icon color="primary" mb="2.5px" as={FaCaretRight} />;
};

export default CaretIcon;
