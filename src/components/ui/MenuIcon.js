import { Icon } from "@chakra-ui/react";

import { FaBars } from "react-icons/fa";

const MenuIcon = () => {
  return <Icon color="gray.700" h={21} w={21} as={FaBars} />;
};

export default MenuIcon;
